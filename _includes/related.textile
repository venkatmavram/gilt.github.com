Related Posts:<br/>
{% for post in site.related_posts %}
  {% if post.id != page.id %}
    {% if post.categories == empty or post.categories contains 'frontpage' or (post.categories contains 'events' and page.categories contains 'events') %}
        <span>{{ post.date | date_to_string }}</span> &raquo; <a href="{{ post.url }}">{{ post.title }}</a><br/>
    {% endif %}
  {% endif %}
{% endfor %}